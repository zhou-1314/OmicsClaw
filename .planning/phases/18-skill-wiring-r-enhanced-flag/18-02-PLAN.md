---
phase: 18-skill-wiring-r-enhanced-flag
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/singlecell/scrna/sc-preprocessing/sc_preprocessing.py
  - skills/singlecell/scrna/sc-batch-integration/sc_integrate.py
  - skills/singlecell/scrna/sc-clustering/sc_clustering.py
  - skills/singlecell/scrna/sc-doublet-detection/sc_doublet_detection.py
  - skills/singlecell/scrna/sc-qc/sc_qc.py
  - skills/singlecell/scrna/sc-pathway-scoring/sc_pathway_scoring.py
autonomous: true
requirements: [SK-11]

must_haves:
  truths:
    - "All 6 Group B first-half skills accept --r-enhanced without error"
    - "Running with --r-enhanced creates figures/r_enhanced/ and attempts shared embedding plots"
    - "R failure (missing embedding CSV) emits warning and does not crash the skill"
    - "Running without --r-enhanced is identical to pre-Phase-18 behavior"
  artifacts:
    - path: "skills/singlecell/scrna/sc-preprocessing/sc_preprocessing.py"
      provides: "--r-enhanced, R_ENHANCED_PLOTS with shared embedding renderers"
      contains: "plot_embedding_discrete"
    - path: "skills/singlecell/scrna/sc-batch-integration/sc_integrate.py"
      provides: "--r-enhanced, R_ENHANCED_PLOTS with shared embedding renderers"
      contains: "plot_embedding_discrete"
    - path: "skills/singlecell/scrna/sc-clustering/sc_clustering.py"
      provides: "--r-enhanced, R_ENHANCED_PLOTS with shared embedding renderers"
      contains: "plot_embedding_discrete"
    - path: "skills/singlecell/scrna/sc-doublet-detection/sc_doublet_detection.py"
      provides: "--r-enhanced, R_ENHANCED_PLOTS with shared embedding renderers"
      contains: "plot_embedding_discrete"
    - path: "skills/singlecell/scrna/sc-qc/sc_qc.py"
      provides: "--r-enhanced, R_ENHANCED_PLOTS"
      contains: "R_ENHANCED_PLOTS"
    - path: "skills/singlecell/scrna/sc-pathway-scoring/sc_pathway_scoring.py"
      provides: "--r-enhanced, R_ENHANCED_PLOTS"
      contains: "R_ENHANCED_PLOTS"
  key_links:
    - from: "main() in each skill"
      to: "_render_r_enhanced()"
      via: "called after Python figures complete"
      pattern: "_render_r_enhanced"
---

<objective>
Wire --r-enhanced into 6 Group B skills (first half): sc-preprocessing, sc-batch-integration,
sc-clustering, sc-doublet-detection, sc-qc, and sc-pathway-scoring.

Purpose: Group B skills only use the shared embedding renderers (plot_embedding_discrete,
plot_embedding_feature) from embedding.R. The pattern is identical to Group A; the only
difference is the R_ENHANCED_PLOTS dict contents.

NOTE on embedding CSVs: Some Group B skills export embedding CSVs to figure_data/ (e.g.,
sc-batch-integration writes umap_points.csv, sc-doublet-detection writes embedding_points.csv).
Others (sc-qc, sc-pathway-scoring) do not produce embedding data. For skills without
embedding CSVs, embedding renderers will gracefully fail with a warning — this is expected
and correct behavior. No CSV-generation work is needed in this plan.

Output: 6 modified Python skill files all accepting --r-enhanced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-r-enhanced-framework-foundation/13-01-SUMMARY.md

<interfaces>
<!-- Exact wiring pattern — same as Plan 18-01 -->

## R_ENHANCED_PLOTS for every Group B first-half skill (use these exact values):

sc-preprocessing:
  R_ENHANCED_PLOTS = {
      "plot_embedding_discrete": "r_embedding_discrete.png",
      "plot_embedding_feature":  "r_embedding_feature.png",
  }

sc-batch-integration:
  R_ENHANCED_PLOTS = {
      "plot_embedding_discrete": "r_embedding_discrete.png",
      "plot_embedding_feature":  "r_embedding_feature.png",
  }

sc-clustering:
  R_ENHANCED_PLOTS = {
      "plot_embedding_discrete": "r_embedding_discrete.png",
      "plot_embedding_feature":  "r_embedding_feature.png",
  }

sc-doublet-detection:
  R_ENHANCED_PLOTS = {
      "plot_embedding_discrete": "r_embedding_discrete.png",
      "plot_embedding_feature":  "r_embedding_feature.png",
  }

sc-qc:
  R_ENHANCED_PLOTS = {
      "plot_embedding_discrete": "r_embedding_discrete.png",
  }
  # sc-qc may not produce UMAP — only discrete embedding if available.
  # R failure is gracefully handled by call_r_plot() warning mechanism.

sc-pathway-scoring:
  R_ENHANCED_PLOTS = {
      "plot_embedding_discrete": "r_embedding_discrete.png",
      "plot_embedding_feature":  "r_embedding_feature.png",
  }

## Standard 5-step wiring pattern (same as Plan 18-01):

Step 1: Add R_ENHANCED_PLOTS dict after imports
Step 2: Add --r-enhanced to argparse parser in main()
Step 3: Add _render_r_enhanced() function:

  def _render_r_enhanced(output_dir, figure_data_dir, r_enhanced):
      if not r_enhanced:
          return []
      from skills.singlecell._lib.viz.r import call_r_plot
      r_figures_dir = output_dir / "figures" / "r_enhanced"
      r_figures_dir.mkdir(parents=True, exist_ok=True)
      r_figure_paths = []
      for renderer, filename in R_ENHANCED_PLOTS.items():
          out_path = r_figures_dir / filename
          call_r_plot(renderer, figure_data_dir, out_path)
          if out_path.exists():
              r_figure_paths.append(str(out_path))
      return r_figure_paths

Step 4: Call _render_r_enhanced() in main() AFTER Python figures and write_result_json
Step 5: result_data["r_enhanced_figures"] = r_enhanced_figures (add BEFORE write_result_json)

## Key invariants:
- Lazy import of call_r_plot inside _render_r_enhanced() only
- R failure is a warning, never an exception — do not add try/except around _render_r_enhanced()
  call; call_r_plot() already handles this internally
- figures/r_enhanced/ path: output_dir / "figures" / "r_enhanced"  (NOT top-level r_enhanced/)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire --r-enhanced into sc-preprocessing, sc-batch-integration, sc-clustering</name>
  <files>
    skills/singlecell/scrna/sc-preprocessing/sc_preprocessing.py,
    skills/singlecell/scrna/sc-batch-integration/sc_integrate.py,
    skills/singlecell/scrna/sc-clustering/sc_clustering.py
  </files>
  <action>
Read each file first to locate: (a) the imports block end, (b) the argparse parser definition
in main(), (c) where write_result_json / write_standard_run_artifacts is called in main().

Then apply the 5-step wiring pattern from the interfaces block to each file.

For sc_preprocessing.py:
- R_ENHANCED_PLOTS: embedding_discrete + embedding_feature
- Find where main() ends its figure rendering; call _render_r_enhanced() after it
- sc-preprocessing likely calls write_standard_run_artifacts or similar; place call after that

For sc_integrate.py (sc-batch-integration):
- R_ENHANCED_PLOTS: embedding_discrete + embedding_feature
- sc-batch-integration exports umap_points.csv to figure_data/ — the embedding renderers
  may actually succeed here. figure_data_dir = output_dir / "figure_data"
- Find write_result_json call in main() and add _render_r_enhanced() call just before
  result_data["r_enhanced_figures"] assignment

For sc_clustering.py:
- R_ENHANCED_PLOTS: embedding_discrete + embedding_feature
- Apply standard pattern

In all three files:
- Place result_data["r_enhanced_figures"] = r_enhanced_figures just before write_result_json()
  so the key is included in result.json
- The _render_r_enhanced() call itself comes AFTER write_standard_run_artifacts / the final
  success banner print — it is purely additive and does not block the normal completion path
  </action>
  <verify>
    <automated>
python skills/singlecell/scrna/sc-preprocessing/sc_preprocessing.py --demo --output /tmp/test_18_preproc --r-enhanced 2>&1 | tail -5
ls /tmp/test_18_preproc/figures/r_enhanced/ 2>/dev/null || echo "MISSING r_enhanced"
python skills/singlecell/scrna/sc-batch-integration/sc_integrate.py --demo --output /tmp/test_18_batch --r-enhanced 2>&1 | tail -5
ls /tmp/test_18_batch/figures/r_enhanced/ 2>/dev/null || echo "MISSING r_enhanced"
python -c "import json; d=json.load(open('/tmp/test_18_batch/result.json')); print('r_enhanced_figures:', d.get('data',{}).get('r_enhanced_figures','KEY MISSING'))"
    </automated>
  </verify>
  <done>
- All three skills run --demo --r-enhanced without unhandled exception
- figures/r_enhanced/ directory exists after run
- result.json contains r_enhanced_figures key
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire --r-enhanced into sc-doublet-detection, sc-qc, sc-pathway-scoring</name>
  <files>
    skills/singlecell/scrna/sc-doublet-detection/sc_doublet_detection.py,
    skills/singlecell/scrna/sc-qc/sc_qc.py,
    skills/singlecell/scrna/sc-pathway-scoring/sc_pathway_scoring.py
  </files>
  <action>
Read each file first to understand structure, then apply the 5-step wiring pattern.

For sc_doublet_detection.py:
- R_ENHANCED_PLOTS: embedding_discrete + embedding_feature
- sc-doublet-detection exports embedding_points.csv to figure_data/ — R renderers may succeed
- Standard pattern: argparse flag, R_ENHANCED_PLOTS, _render_r_enhanced(), call in main()

For sc_qc.py:
- R_ENHANCED_PLOTS: only embedding_discrete (sc-qc likely has no UMAP in figure_data —
  R renderer will gracefully warn and produce no PNG, which is fine)
- Standard pattern unchanged

For sc_pathway_scoring.py:
- R_ENHANCED_PLOTS: embedding_discrete + embedding_feature
- Standard pattern

In all three:
- result_data["r_enhanced_figures"] = r_enhanced_figures added before write_result_json
- _render_r_enhanced() called after Python figures

NOTE for sc-qc: This skill focuses on QC metrics (violin plots, scatter plots of QC metrics).
It does not produce a UMAP embedding in demo mode. When _render_r_enhanced() calls
plot_embedding_discrete, call_r_plot() will warn "figure_data CSV not found" and return
without creating a PNG. This is correct and expected — do NOT suppress this warning.
  </action>
  <verify>
    <automated>
python skills/singlecell/scrna/sc-doublet-detection/sc_doublet_detection.py --demo --output /tmp/test_18_dbl --r-enhanced 2>&1 | tail -5
ls /tmp/test_18_dbl/figures/r_enhanced/ 2>/dev/null || echo "MISSING r_enhanced"
python skills/singlecell/scrna/sc-qc/sc_qc.py --demo --output /tmp/test_18_qc --r-enhanced 2>&1 | tail -5
echo "Exit code: $?"
python skills/singlecell/scrna/sc-pathway-scoring/sc_pathway_scoring.py --demo --output /tmp/test_18_path --r-enhanced 2>&1 | tail -5
ls /tmp/test_18_path/figures/r_enhanced/ 2>/dev/null || echo "MISSING r_enhanced"
    </automated>
  </verify>
  <done>
- All three skills run --demo --r-enhanced without unhandled exception (R warnings are OK)
- figures/r_enhanced/ directory created for all three
- result.json r_enhanced_figures key present (may be empty list for sc-qc — that is correct)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python -> R subprocess | call_r_plot() invokes Rscript; output is a PNG or a warning |
| figure_data/ absence | Skills without embedding CSVs: R renderer silently fails with warning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Denial of Service | R hang on skills without embedding data | mitigate | RScriptRunner timeout=120 already applies; call_r_plot wraps as warning |
| T-18-06 | Spoofing | R_ENHANCED_PLOTS maps renderer not in registry | mitigate | registry.R exits 1 for unknown renderer; Python warning captures cleanly |
| T-18-07 | Repudiation | r_enhanced_figures=[] in result.json misleads | accept | empty list is accurate — skill ran, R had nothing to render (no embedding data) |
</threat_model>

<verification>
```bash
for skill in sc-preprocessing sc-batch-integration sc-clustering sc-doublet-detection sc-qc sc-pathway-scoring; do
  echo "=== $skill ==="
  python omicsclaw.py run $skill --demo --output /tmp/18b_$skill --r-enhanced 2>&1 | grep -E "Success|Warning|Error" | head -3
  ls /tmp/18b_$skill/figures/r_enhanced/ 2>/dev/null && echo "r_enhanced dir OK" || echo "MISSING"
  python -c "
import json, sys
try:
    d = json.load(open('/tmp/18b_$skill/result.json'))
    key_present = 'r_enhanced_figures' in d.get('data', {})
    print('r_enhanced_figures key:', key_present)
except Exception as e:
    print('result.json error:', e)
"
done
```
</verification>

<success_criteria>
- All 6 Group B first-half skills accept --r-enhanced without crash
- figures/r_enhanced/ directory created in each output dir when --r-enhanced is passed
- result.json contains r_enhanced_figures key for each skill (list may be empty for qc/pathway if no embedding data)
- Without --r-enhanced: identical behavior to pre-Phase-18 (no regression in Python figures)
</success_criteria>

<output>
After completion, create `.planning/phases/18-skill-wiring-r-enhanced-flag/18-02-SUMMARY.md`
following the standard summary template.
</output>
