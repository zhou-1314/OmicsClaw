---
phase: 18-skill-wiring-r-enhanced-flag
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/singlecell/scrna/sc-grn/sc_grn.py
  - skills/singlecell/scrna/sc-gene-programs/sc_gene_programs.py
  - skills/singlecell/scrna/sc-metacell/sc_metacell.py
  - skills/singlecell/scrna/sc-perturb/sc_perturb.py
  - skills/singlecell/scrna/sc-cytotrace/sc_cytotrace.py
  - docs/R-DEPENDENCIES.md
autonomous: true
requirements: [SK-11]

must_haves:
  truths:
    - "Remaining 5 Group B skills accept --r-enhanced without error"
    - "docs/R-DEPENDENCIES.md exists with actionable install commands based on real experience"
    - "R-DEPENDENCIES.md documents the ggplot2 3.5.2 pin, monocle3 GitHub install, GSVA BiocManager, magick linker flag"
    - "All 19 targeted skills (Group A + Group B) are wired after Plans 01-03 complete"
  artifacts:
    - path: "skills/singlecell/scrna/sc-grn/sc_grn.py"
      provides: "--r-enhanced, R_ENHANCED_PLOTS with shared embedding renderers"
      contains: "R_ENHANCED_PLOTS"
    - path: "skills/singlecell/scrna/sc-gene-programs/sc_gene_programs.py"
      provides: "--r-enhanced, R_ENHANCED_PLOTS"
      contains: "R_ENHANCED_PLOTS"
    - path: "skills/singlecell/scrna/sc-metacell/sc_metacell.py"
      provides: "--r-enhanced, R_ENHANCED_PLOTS"
      contains: "R_ENHANCED_PLOTS"
    - path: "skills/singlecell/scrna/sc-perturb/sc_perturb.py"
      provides: "--r-enhanced, R_ENHANCED_PLOTS"
      contains: "R_ENHANCED_PLOTS"
    - path: "skills/singlecell/scrna/sc-cytotrace/sc_cytotrace.py"
      provides: "--r-enhanced, R_ENHANCED_PLOTS"
      contains: "R_ENHANCED_PLOTS"
    - path: "docs/R-DEPENDENCIES.md"
      provides: "Actionable R package install guide from real experience"
      contains: "ggplot2"
  key_links:
    - from: "docs/R-DEPENDENCIES.md"
      to: "docs/INSTALLATION.md"
      via: "referenced as 'For R Enhanced: see docs/R-DEPENDENCIES.md'"
      pattern: "R-DEPENDENCIES"
---

<objective>
Wire --r-enhanced into 5 remaining Group B skills (sc-grn, sc-gene-programs, sc-metacell,
sc-perturb, sc-cytotrace) and create docs/R-DEPENDENCIES.md with real installation experience.

Purpose: Completes SK-11 coverage. All 19 skills wired after this plan. The R-DEPENDENCIES.md
documents what we learned about R package installation during Phases 13-17: ggplot2 pin,
monocle3 GitHub install, GSVA via BiocManager, magick linker issue, etc.

Output: 5 modified Python skill files + docs/R-DEPENDENCIES.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-r-enhanced-framework-foundation/13-01-SUMMARY.md

<interfaces>
## R_ENHANCED_PLOTS for Group B second-half skills:

sc-grn:
  R_ENHANCED_PLOTS = {
      "plot_embedding_discrete": "r_embedding_discrete.png",
  }
  # sc-grn focuses on GRN adjacency/regulon data. No UMAP-like embedding in figure_data.
  # Only plot_embedding_discrete for any embedding present; graceful warning if absent.

sc-gene-programs:
  R_ENHANCED_PLOTS = {
      "plot_embedding_discrete": "r_embedding_discrete.png",
      "plot_embedding_feature":  "r_embedding_feature.png",
  }

sc-metacell:
  R_ENHANCED_PLOTS = {
      "plot_embedding_discrete": "r_embedding_discrete.png",
  }
  # sc-metacell exports points_df to figure_data — may have coordinates usable as embedding.

sc-perturb:
  R_ENHANCED_PLOTS = {
      "plot_embedding_discrete": "r_embedding_discrete.png",
      "plot_embedding_feature":  "r_embedding_feature.png",
  }

sc-cytotrace:
  R_ENHANCED_PLOTS = {
      "plot_embedding_discrete": "r_embedding_discrete.png",
      "plot_embedding_feature":  "r_embedding_feature.png",
  }

## Standard 5-step wiring pattern (same as Plans 18-01 and 18-02 — see those plans for
## the full _render_r_enhanced() function body, argparse pattern, and invariants).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire --r-enhanced into sc-grn, sc-gene-programs, sc-metacell, sc-perturb, sc-cytotrace</name>
  <files>
    skills/singlecell/scrna/sc-grn/sc_grn.py,
    skills/singlecell/scrna/sc-gene-programs/sc_gene_programs.py,
    skills/singlecell/scrna/sc-metacell/sc_metacell.py,
    skills/singlecell/scrna/sc-perturb/sc_perturb.py,
    skills/singlecell/scrna/sc-cytotrace/sc_cytotrace.py
  </files>
  <action>
Read each file first to understand structure (imports, main(), write_result_json location).
Then apply the 5-step wiring pattern (same as Plans 18-01 and 18-02).

For each skill:
1. Add R_ENHANCED_PLOTS dict after imports (use the per-skill values from interfaces block)
2. Add --r-enhanced flag to argparse parser in main()
3. Add _render_r_enhanced() function:
   def _render_r_enhanced(output_dir, figure_data_dir, r_enhanced):
       if not r_enhanced:
           return []
       from skills.singlecell._lib.viz.r import call_r_plot
       r_figures_dir = output_dir / "figures" / "r_enhanced"
       r_figures_dir.mkdir(parents=True, exist_ok=True)
       r_figure_paths = []
       for renderer, filename in R_ENHANCED_PLOTS.items():
           out_path = r_figures_dir / filename
           call_r_plot(renderer, figure_data_dir, out_path)
           if out_path.exists():
               r_figure_paths.append(str(out_path))
       return r_figure_paths
4. Add result_data["r_enhanced_figures"] = [] before write_result_json (placeholder)
5. After write_result_json / write_standard_run_artifacts, call:
   r_enhanced_figures = _render_r_enhanced(output_dir, output_dir / "figure_data", args.r_enhanced)
   Note: update result_data["r_enhanced_figures"] = r_enhanced_figures BEFORE write_result_json
   if that is architecturally possible; otherwise add a second result_data update if not.

Special notes per skill:
- sc-grn: The figure_data/ has top_adjacencies.csv and regulon_summary.csv — no embedding.
  plot_embedding_discrete will warn gracefully. This is expected and fine.
- sc-gene-programs: figure_data has usage_df and top_df (program activity). No UMAP likely.
  R warnings expected and fine.
- sc-metacell: figure_data has points_df (centroid coordinates). R renderer may fail — OK.
- sc-perturb: figure_data has mixscape_global_classes.csv and mixscape_class_counts.csv.
  No embedding CSV — R warnings expected.
- sc-cytotrace: May have an embedding CSV (cytotrace score overlay on UMAP).
  Check file for "embedding.csv" or "umap" in figure_data exports and use that if found.

All R failures are warnings, never exceptions. Do not add try/except around the call.
  </action>
  <verify>
    <automated>
python skills/singlecell/scrna/sc-grn/sc_grn.py --demo --output /tmp/test_18_grn --r-enhanced 2>&1 | tail -5
echo "Exit: $?"
python skills/singlecell/scrna/sc-gene-programs/sc_gene_programs.py --demo --output /tmp/test_18_gp --r-enhanced 2>&1 | tail -5
echo "Exit: $?"
python skills/singlecell/scrna/sc-metacell/sc_metacell.py --demo --output /tmp/test_18_meta --r-enhanced 2>&1 | tail -5
echo "Exit: $?"
python skills/singlecell/scrna/sc-perturb/sc_perturb.py --demo --output /tmp/test_18_perturb --r-enhanced 2>&1 | tail -5
echo "Exit: $?"
    </automated>
  </verify>
  <done>
- All 5 skills run --demo --r-enhanced without unhandled exception (exit 0, R warnings are OK)
- figures/r_enhanced/ directory created for each skill
- result.json contains r_enhanced_figures key in each output
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docs/R-DEPENDENCIES.md</name>
  <files>docs/R-DEPENDENCIES.md</files>
  <action>
Create docs/R-DEPENDENCIES.md documenting real R package installation experience from
Phases 13-17. This is the authoritative install guide for OmicsClaw R Enhanced features.

The file must cover the following sections (in this order):

## 1. Overview
Explain that OmicsClaw R Enhanced plotting requires specific R packages. The OMICSCLAW_R_LIBS
environment variable points to a dedicated library directory (under $CONDA_PREFIX/lib/R/omicsclaw-library)
that is searched first, allowing version pinning that shadows conda-managed R packages.

## 2. Prerequisites
- R must be installed (4.x). Check: `Rscript --version`
- Active conda environment: `conda activate <your_env>`
- OMICSCLAW_R_LIBS is set automatically by RScriptRunner when conda is active

## 3. Core Packages (required for all R Enhanced plots)

```r
# Run in R or Rscript -e "..."
r_libs <- Sys.getenv("OMICSCLAW_R_LIBS")
if (nchar(r_libs) == 0) stop("OMICSCLAW_R_LIBS not set — activate conda env first")

# ggplot2 MUST be pinned to 3.5.2 — 4.0.2 breaks the + operator (S7 migration)
# Do NOT use install.packages("ggplot2") — that installs 4.0.x
install.packages(
  "https://cran.r-project.org/src/contrib/Archive/ggplot2/ggplot2_3.5.2.tar.gz",
  repos = NULL, type = "source", lib = r_libs
)

# Other core dependencies
install.packages(c("scales", "RColorBrewer", "viridis", "ggrepel"), lib = r_libs)

# Verify pin worked:
Rscript -e '.libPaths(c(Sys.getenv("OMICSCLAW_R_LIBS"), .libPaths())); packageVersion("ggplot2")'
# Must print: [1] '3.5.2'   — NOT 4.0.x
```

## 4. ComplexHeatmap (for marker heatmaps and DE heatmaps)

```r
r_libs <- Sys.getenv("OMICSCLAW_R_LIBS")
if (!requireNamespace("BiocManager", quietly = TRUE))
    install.packages("BiocManager", lib = r_libs)
BiocManager::install("ComplexHeatmap", lib = r_libs, update = FALSE, ask = FALSE)
```

## 5. magick (for image processing — required by some renderers)

magick requires system library libmagick++-dev. If you see linker errors:

```bash
# Ubuntu/Debian
sudo apt-get install -y libmagick++-dev

# Then install from source with the linker flag:
Rscript -e 'install.packages("magick", lib=Sys.getenv("OMICSCLAW_R_LIBS"), type="source")'
```

If system install is not possible, magick-dependent renderers will warn and skip — other
renderers continue normally.

## 6. monocle3 (for sc-pseudotime --method monocle3_r)

monocle3 is on GitHub (not CRAN). Required for LineagePlot/DynamicPlot renderers.

```r
r_libs <- Sys.getenv("OMICSCLAW_R_LIBS")
# Dependencies
if (!requireNamespace("BiocManager", quietly = TRUE))
    install.packages("BiocManager", lib = r_libs)
BiocManager::install(c("BiocGenerics", "DelayedArray", "DelayedMatrixStats",
                        "limma", "lme4", "S4Vectors", "SingleCellExperiment",
                        "SummarizedExperiment", "batchelor", "HDF5Array",
                        "terra", "ggrastr"), lib = r_libs, update = FALSE, ask = FALSE)
# leidenbase from CRAN
install.packages("leidenbase", lib = r_libs)
# monocle3 from GitHub
if (!requireNamespace("remotes", quietly = TRUE))
    install.packages("remotes", lib = r_libs)
remotes::install_github("cole-trapnell-lab/monocle3", lib = r_libs)
```

## 7. GSVA (for sc-enrichment --method gsva_r)

```r
r_libs <- Sys.getenv("OMICSCLAW_R_LIBS")
if (!requireNamespace("BiocManager", quietly = TRUE))
    install.packages("BiocManager", lib = r_libs)
BiocManager::install("GSVA", lib = r_libs, update = FALSE, ask = FALSE)
```

## 8. clusterProfiler / fgsea (for sc-enrichment --method gsea_r)

Already installed as part of Phase 15. Verify:

```r
Rscript -e 'library(clusterProfiler); library(fgsea); cat("OK\n")'
```

If missing:
```r
r_libs <- Sys.getenv("OMICSCLAW_R_LIBS")
BiocManager::install(c("clusterProfiler", "fgsea"), lib = r_libs, update = FALSE, ask = FALSE)
```

## 9. Troubleshooting

### ggplot2 + operator error: "non-numeric argument to binary operator"
Cause: ggplot2 4.0.x S7 migration broke ggproto + operator.
Fix: Pin 3.5.2 per Section 3 above. Verify: `packageVersion("ggplot2")` must return `3.5.2`.

### "there is no package called 'X'"
Cause: OMICSCLAW_R_LIBS not in .libPaths() when R script runs.
Fix: Ensure conda env is active. RScriptRunner sets R_LIBS_USER automatically.
Manual check: `Rscript -e 'cat(.libPaths(), sep="\n")'` — first entry should be omicsclaw-library.

### R Enhanced plots produce warnings but no PNG
Expected for skills that do not export embedding CSVs to figure_data/. The R renderer
gracefully warns and skips — Python figures are unaffected.

### monocle3 install fails on BiocGenerics version conflict
Use `ask = FALSE, force = TRUE` in BiocManager::install() to allow updates.

## 10. Minimal install (just ggplot2 + ComplexHeatmap for most plots)

```bash
# One-liner for CI or fresh machines:
Rscript -e '
r_libs <- Sys.getenv("OMICSCLAW_R_LIBS")
if (nchar(r_libs) == 0) stop("Set OMICSCLAW_R_LIBS or activate conda env")
install.packages("https://cran.r-project.org/src/contrib/Archive/ggplot2/ggplot2_3.5.2.tar.gz",
  repos=NULL, type="source", lib=r_libs)
install.packages(c("scales","RColorBrewer","viridis","ggrepel"), lib=r_libs)
if (!requireNamespace("BiocManager",quietly=TRUE)) install.packages("BiocManager",lib=r_libs)
BiocManager::install("ComplexHeatmap", lib=r_libs, update=FALSE, ask=FALSE)
cat("Core R Enhanced packages installed\n")
'
```

---
*Last updated: Phase 18 (2026-04-11). Based on real install experience from Phases 13-17.*

Write the above content verbatim to docs/R-DEPENDENCIES.md. Then add a cross-reference line
to docs/INSTALLATION.md: find the "R" section (or "optional" section) and add a line:
"For R Enhanced plotting dependencies, see [docs/R-DEPENDENCIES.md](R-DEPENDENCIES.md)."
Read docs/INSTALLATION.md first to find the right insertion point.
  </action>
  <verify>
    <automated>
test -f /data2/liying/git/OmicsClaw/docs/R-DEPENDENCIES.md && echo "File exists" || echo "MISSING"
grep -c "ggplot2_3.5.2" /data2/liying/git/OmicsClaw/docs/R-DEPENDENCIES.md
grep -c "monocle3" /data2/liying/git/OmicsClaw/docs/R-DEPENDENCIES.md
grep -c "GSVA" /data2/liying/git/OmicsClaw/docs/R-DEPENDENCIES.md
grep "R-DEPENDENCIES" /data2/liying/git/OmicsClaw/docs/INSTALLATION.md && echo "Cross-ref OK" || echo "Cross-ref MISSING"
    </automated>
  </verify>
  <done>
- docs/R-DEPENDENCIES.md exists with all 10 sections
- Contains ggplot2 3.5.2 pin command, monocle3 GitHub install, GSVA BiocManager, magick linker note
- docs/INSTALLATION.md references docs/R-DEPENDENCIES.md
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python -> R subprocess | same as Plans 01-02 |
| docs/R-DEPENDENCIES.md -> user | install commands executed by user on their machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Denial of Service | R renderer warnings on skills with no embedding data | accept | expected behavior; warnings are informational, skill exits 0 |
| T-18-09 | Spoofing | R-DEPENDENCIES.md install commands download from CRAN/GitHub | accept | standard R package installation channels; user must review before running |
| T-18-10 | Tampering | monocle3 GitHub install (remotes::install_github) | accept | monocle3 has no CRAN release; GitHub is the official source (cole-trapnell-lab) |
</threat_model>

<verification>
```bash
# Verify all 19 skills are wired (Plans 01 + 02 + 03)
for skill in \
  sc-cell-annotation sc-de sc-markers sc-enrichment sc-pseudotime \
  sc-cell-communication sc-velocity sc-differential-abundance \
  sc-preprocessing sc-batch-integration sc-clustering sc-doublet-detection \
  sc-qc sc-pathway-scoring \
  sc-grn sc-gene-programs sc-metacell sc-perturb sc-cytotrace; do
  pyfile=$(ls /data2/liying/git/OmicsClaw/skills/singlecell/scrna/$skill/*.py 2>/dev/null | grep -v test | head -1)
  [ -z "$pyfile" ] && echo "MISSING pyfile for $skill" && continue
  grep -q "R_ENHANCED_PLOTS" "$pyfile" && echo "OK: $skill" || echo "MISSING R_ENHANCED_PLOTS: $skill"
done

# Verify docs/R-DEPENDENCIES.md
grep -c "ggplot2_3.5.2" /data2/liying/git/OmicsClaw/docs/R-DEPENDENCIES.md
grep "R-DEPENDENCIES" /data2/liying/git/OmicsClaw/docs/INSTALLATION.md
```
</verification>

<success_criteria>
- All 5 Group B second-half skills wired with --r-enhanced (exit 0, figures/r_enhanced/ created)
- Total: all 19 targeted skills have R_ENHANCED_PLOTS dict and --r-enhanced flag
- docs/R-DEPENDENCIES.md exists with actionable install commands (ggplot2 pin, monocle3, GSVA, magick)
- docs/INSTALLATION.md cross-references R-DEPENDENCIES.md
</success_criteria>

<output>
After completion, create `.planning/phases/18-skill-wiring-r-enhanced-flag/18-03-SUMMARY.md`
following the standard summary template.
</output>
