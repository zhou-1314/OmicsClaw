# Conda environment for the LCA R subprocess.
# Adapted from SACCELERATOR consensus/03_Consensus_lca/Consensus_lca.yaml.
# Lift bounds slightly to track conda-forge "latest known good".
channels:
  - r
  - conda-forge
dependencies:
  - r-base=4.4.2
  - r-optparse=1.7.5
  - r-dicer=2.2.0
  - r-polca=1.6.0
