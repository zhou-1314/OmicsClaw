You are extracting structured findings from one member of a multi-method
consensus run. The member ran skill ``{skill_name}`` on the user's data.
Read the report below and produce a JSON object with EXACTLY these keys:

- key_findings (list of strings): 3-7 concise bullet points
- key_numbers (object mapping metric name → numeric or string value)
- confidence (one of "high" / "medium" / "low" — your honest assessment
  of how trustworthy this member's output looks given the report content)
- caveats (list of strings, may be empty): explicit warnings or "soft"
  results the synthesiser should weight down

Constraints:
- DO NOT invent numbers not present in the report.
- DO NOT speculate beyond what the report says.
- Prefer extracting exact metric names as written in the report.
- Output ONLY the JSON object. No prose around it.

Member name: {member_name}
Member skill: {skill_name}
Report:
---
{report_text}
---
