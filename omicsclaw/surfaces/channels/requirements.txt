# OmicsClaw Bot dependencies
# ─────────────────────────────────────────────────────────────────────
#
# INSTALL (core only):
#     pip install -r bot/requirements.txt
#
# INSTALL (all channels):
#     pip install -r bot/requirements.txt -r bot/requirements-channels.txt
#
# Or install individual channel extras — see bot/README.md.
# ─────────────────────────────────────────────────────────────────────

# ── Shared core ──────────────────────────────────────────────────────
openai>=1.12.0
httpx[socks]>=0.25.0
python-dotenv>=1.0.0

# ── TTS (optional — for voice replies) ───────────────────────────────
edge-tts>=6.1.0

# ── Onboarding / CLI ─────────────────────────────────────────────────
rich>=13.0.0
questionary>=2.0.0
textual>=0.85.0
