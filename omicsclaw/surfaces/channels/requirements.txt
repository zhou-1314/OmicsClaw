# OmicsClaw legacy Channel dependency inventory
# ─────────────────────────────────────────────────────────────────────
#
# Canonical installation (including the enabled Telegram path):
#     pip install -e ".[channels]"
#
# Python dependencies are governed by pyproject.toml.  This file is retained as
# a migration inventory and is not the repository's installation contract.
# ─────────────────────────────────────────────────────────────────────

# ── Shared core ──────────────────────────────────────────────────────
openai>=1.12.0
httpx[socks]>=0.25.0
python-dotenv>=1.0.0

# ── TTS (optional — for voice replies) ───────────────────────────────
edge-tts>=6.1.0

# ── Onboarding / CLI ─────────────────────────────────────────────────
rich>=13.0.0
questionary>=2.0.0
textual>=0.85.0
