You are recommending the next analysis step(s) AFTER a verified typed
consensus run. Your recommendations MUST be backward proof-driven:
every recommendation cites at least one specific artifact row from the
typed run as evidence.

# Available downstream OmicsClaw skills (pick from this list ONLY)

- spatial-de            : per-group differential expression
- spatial-deconv        : reference-based cell-type deconvolution
- spatial-communication : ligand-receptor analysis
- spatial-trajectory    : pseudotime / trajectory inference
- spatial-cnv           : copy-number inference
- spatial-velocity      : RNA velocity
- spatial-statistics    : neighborhood enrichment / spatial autocorrelation
- spatial-genes         : spatially variable genes
- spatial-condition     : condition / treatment comparison

# Annotation summary (one row per consensus cluster)

{annotation_block}

# Cross-method NMI matrix (lowest pairs first — most contested)

{nmi_low_pairs}

# Instructions

Return a SINGLE JSON object containing a `next_steps` array of AT MOST
{top_k} entries, each conforming exactly to this schema. Do not emit
prose outside the JSON.

```json
{{
  "next_steps": [
    {{
      "skill": "<one of the available skills above>",
      "args_hint": "<concrete --flag arguments using consensus_<operator> labels and specific cluster ids>",
      "priority": <1 | 2>,
      "evidence_refs": [
        "<artifact_filename>:<row/col identifier>=<value>"
      ],
      "reason": "<one sentence — must mention specific cluster ids or NMI values from the typed run>"
    }}
  ]
}}
```

# Hard constraints

1. AT MOST {top_k} entries. Prefer quality over quantity.
2. Every entry MUST have non-empty `evidence_refs`. Recommendations
   that cannot cite a specific typed-run artifact value (cluster id,
   NMI pair, mean_local_purity, etc.) MUST be omitted.
3. `priority` must be 1 (cites ≥ 2 distinct artifact rows) or 2 (cites
   ≥ 1 artifact row). Priority 3 ("weak — only cluster id") MUST NOT
   appear; drop those candidates instead.
4. `skill` MUST be one of the listed downstream OmicsClaw skills.
